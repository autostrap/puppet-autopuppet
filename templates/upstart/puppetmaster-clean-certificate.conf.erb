# puppetmaster-clean-certificate
#
# Deletes its own puppet certificate and tells the master to to do soo

description "puppetmaster-clean-certificate"

#start on (runlevel [2345]
#  or starting networking)
stop on runlevel [016]
stop on stopping networking

pre-start script
  curl -k --cert /var/lib/puppet/ssl/certs/<%= @fqdn -%>.pem --key /var/lib/puppet/ssl/private_keys/<%= @fqdn -%>.pem https://<%= @puppet_master -%>:2342
  rm -rf /var/lib/puppet/ssl
end script

